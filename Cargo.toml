[package]
name = "zenjxl"
version = "0.1.0"
edition = "2024"
rust-version = "1.93"
license = "AGPL-3.0-only OR LicenseRef-Imazen-Commercial"
description = "JPEG XL encoding and decoding with zencodec trait integration"
categories = ["multimedia::images", "multimedia::encoding"]
keywords = ["jxl", "jpeg-xl", "image", "encoder", "decoder"]
repository = "https://github.com/imazen/zenjxl"
exclude = ["CLAUDE.md", ".claude/", ".github/"]

[dependencies]
thiserror = { version = "2.0.18", default-features = false }
whereat = "0.1.5"
rgb = { version = "0.8.53", default-features = false, features = ["as-bytes"] }
imgref = "1.12.0"
enough = "0.4.3"
zencodec = "0.1.8"
zenpixels = { version = "0.2.1", default-features = false, features = ["rgb"] }
linear-srgb = "0.6.6"
jxl = { path = "../zenjxl-decoder/zenjxl-decoder", package = "zenjxl-decoder", optional = true }
jxl-encoder = { path = "../jxl-encoder/jxl-encoder", optional = true, default-features = false, features = ["std", "convenience"] }
# zennode = { path = "../zennode/zennode", optional = true, default-features = false, features = ["derive"] }

[dev-dependencies]
archmage = { version = "0.9.15", features = ["std"] }

[features]
default = ["encode", "decode"]
decode = ["dep:jxl"]
threads = ["decode", "jxl/threads"]
encode = ["dep:jxl-encoder"]
butteraugli-loop = ["encode", "jxl-encoder/butteraugli-loop"]
# Enable zencodec trait integration
zencodec = []
# Enable zennode node definitions (EncodeJxl, DecodeJxl)
# zennode = ["dep:zennode"]
